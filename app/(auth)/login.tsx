import { Screen, AppText, Nav } from '@/components'

const LoginScreen = () => {
  return (
    <Screen>
      <Nav />
      <AppText>LoginScreen</AppText>
    </Screen>
  )
}

export default LoginScreen